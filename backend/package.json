{
  "name": "jeevanmitra-backend",
  "version": "1.0.0",
  "description": "Backend API for JeevanMitra AI - proxies chat/vision requests to the Groq API so the API key never lives in the browser.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
